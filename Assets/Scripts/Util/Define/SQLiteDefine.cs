public class SQLiteDefine
{
    public enum Type
    {
        INTEGER,
        TEXT,
        BLOB,
        REAL,
        NUMERIC
    }

    public enum ConstraintType
    {
        PRIMARY_KEY,
        UNIQUE
    }
}
